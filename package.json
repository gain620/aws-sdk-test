{
  "name": "aws-sdk-test",
  "version": "1.0.0",
  "description": "testing aws sdk",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gain Chang",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.416.0"
  }
}
